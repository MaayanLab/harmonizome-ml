<div class="ipynb-field">
    <p>{{ description }}</p>
    <select>
        {% for choice in choices.keys() %}
            <option>{{ choice }}</option>
        {% endfor %}
    </select>
    {% for choice_name, choice in choices.items() %}
        <div class="meta-choice" name="{{ choice_name }}">
            {{ choice.render() }}
        </div>
    {% endfor %}
</div>